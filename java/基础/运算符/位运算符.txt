位运算符
    与&
        两个操作数中位都为1，结果为1，否则结果为0
    非~ 
        如果位为0，结果为1， 如果位为1，结果为0
    或|  
        两个位只要有一个为1，那么结果为1，否则结果为0
    异或^
        两个操作位中，相同结果为0，不同结果为1

移位运算符
    << : 左移运算符
    >> : 右移运算符
    >>>: 无符号右移

掩码：一串二进制代码对目标字段进行位与运算，屏蔽当前的输入位；
    可以理解为将原有数据通过位运算或其他运算进行加密；

